//start the game
//choose which player goes first
//when player clicks an empty gap add O / X depending on which player
//Update the board and check for win
//if no win switch players turn
//repeat step 3
//else if (win) display "Winner Player (1/2)"

let player1 = "X";
let player2 = "O";
let gameBoard = [
["", "", "",],
["", "", "",],
["", "", "",]
];

function makeMove (row, col) {
 if(gameBoard[row][col] == ""){
    gameBoard[row][col] == currentPlayer
    document.getElementsByClassName("")
 }
}